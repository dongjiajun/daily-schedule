# Test Plan: <!-- 变更名称 -->

<!-- 将 delta specs 中每个 `#### Scenario:` 逐条映射到命名测试。
     纪律：
     - 每个场景必须有对应行，Requirement/Scenario 引用与 delta specs 一致，无遗漏
     - 未实现场景 Initial State 记 🔴；实现且对应测试通过后改写 🟢（归档前必须全绿）
     - 无独立测试面的场景（纯配置/手工验证）在 Coverage Notes 注明理由
     - apply 时逐场景先写/改测试（红）再实现至通过（绿）；spec 漂移即停：更新 delta spec 与 test-plan 后继续 -->

| Requirement | Scenario | Test File | Test Name | Initial State | Coverage Notes |
|-------------|----------|-----------|-----------|---------------|----------------|
| <!-- R1 --> | <!-- S1 --> | <!-- 如 backend/src/test/java/... --> | <!-- 测试类/方法名 --> | <!-- 🔴 → 实现后 🟢 --> | <!-- 边界/异常说明；手工验证写明理由 --> |
