# 复制为 application-dev.yml 并填入真实密码
# cp application-dev.example.yml application-dev.yml

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/daily_schedule_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: "请填入你的MySQL密码"
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
